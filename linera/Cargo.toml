[package]
name = "aion-prediction-market"
version = "0.1.0"
edition = "2021"

[dependencies]
linera-sdk = "0.12"
serde = { version = "1.0", features = ["derive"] }
async-trait = "0.1"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
