{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "watchPaths": [
      "backend/**"
    ]
  },
  "deploy": {
    "startCommand": "uvicorn test_server:app --host 0.0.0.0 --port 8000"
  }
}
